server:
  port: 8880

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/spcloud/config-repo.git
          searchPaths: license

encrypt:
  key: IMSYMMETRIC